services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - lottery-data:/root/.lottery
    environment:
      - DEBUG=True
      - ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0
    restart: unless-stopped

volumes:
  lottery-data:
